export default function CalibrationPage() {
  return (
    <div>
      <h1 className="text-xl font-bold">Kalibrasi</h1>
      <p className="mt-2 text-muted-foreground">
        Placeholder — impedance monitoring + per-subject calibration workflow.
      </p>
      {/* TODO: impedance grid display, threshold calibration wizard */}
    </div>
  );
}
