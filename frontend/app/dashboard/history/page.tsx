export default function HistoryPage() {
  return (
    <div>
      <h1 className="text-xl font-bold">Riwayat Sesi</h1>
      <p className="mt-2 text-muted-foreground">
        Placeholder — longitudinal session history table.
      </p>
      {/* TODO: fetch /sessions list, render table with date, decoder, accuracy */}
    </div>
  );
}
