export default function SettingsPage() {
  return (
    <div>
      <h1 className="text-xl font-bold">Pengaturan</h1>
      <p className="mt-2 text-muted-foreground">
        Placeholder — patient ID, operator settings, display preferences.
      </p>
      {/* TODO: form for patient ID, theme toggle, font size, contrast mode */}
    </div>
  );
}
